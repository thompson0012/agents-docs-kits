# Current Focus

Read after `AGENTS.md` when starting or resuming work. Keep this file limited to the current objective and the bounds for active work.

## Objective

Rename the packaged `future-catcher` skill to `strategic-foresight` and tighten when it should trigger.

## Scope

- Rename `templates/base/.agents/skills/future-catcher/` to `templates/base/.agents/skills/strategic-foresight/`.
- Update `SKILL.md` metadata and body so the skill is positioned as signal-driven scenario planning.
- Clarify positive triggers, negative triggers, and the distinction from `domain-expert-consultation`.

## Constraints

- Keep the skill focused on concrete external changes or thresholds plus implications under uncertainty.
- Do not expand it into generic advisory, summary, or implementation guidance.
- Limit work to the packaged skill and the live-state docs for this objective.
- Do not commit from this task.

## Success Criteria

- Only `strategic-foresight` remains under the packaged skill path; `future-catcher` no longer exists.
- The skill frontmatter and body clearly define when to use and when not to use it.
- The live docs record the completed work and verification state.