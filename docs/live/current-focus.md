# Current Focus

Read after `AGENTS.md` when starting or resuming work. Keep this file limited to the current objective and the bounds for active work.

## Objective

Upgrade the template delivery-control control plane in `templates/base/` so it absorbs the strongest portable ideas from gstack, Superpowers, and Compound Engineering while preserving the repo's one-job-per-surface rule.

The template-cutover batch aligned scaffolds and naming. This batch deepens the delivery-control family itself: its harness-design lane, its evaluator lane, and the compound-engineering patterns (durable archives, goal lineage, session compaction) that make multi-session delivery honest.

## Scope

In scope:
- Strengthen the `delivery-control` router and its children in `templates/base/.agents/skills/delivery-control/` with the best portable practices from gstack (browser QA, evidence-gated acceptance), Superpowers (session orchestration, plan execution, verification-before-completion), and Compound Engineering (durable archives in `docs/reference/lessons.md` and `docs/reference/memory.md`, goal lineage in `docs/live/roadmap.md`, session compaction).
- Keep `docs/reference/lessons.md` and `docs/reference/memory.md` as the durable archives; do not invent a parallel archive directory.
- Update live-doc scaffolds in `templates/base/docs/live/` only if the delivery-control upgrade requires new headings or fields — scaffolds remain inert.
- Update the suite router index / category map and website-building follow-on guidance when delivery-control capabilities need to be advertised to the top-level router.

Out of scope:
- Modifying product code, template generation logic, or validator scripts.
- Adding new router families or product-specific skills.
- Filling template scaffolds with real content — they must remain inert structure.
- Reopening the root docs cutover or the template-cutover batch — both are complete.

## Constraints

- One concept, one representation — no compatibility shims, no duplicate authorities.
- Live docs must be self-contained: an agent resumes from these files, not chat memory.
- Template scaffolds remain neutral scaffolds with no seeded project facts.
- The compound layer uses existing `docs/reference/lessons.md` and `docs/reference/memory.md` as durable archives — no parallel directory.

## Success Criteria

- The `delivery-control` family in `templates/base/` carries the strongest portable patterns from gstack, Superpowers, and Compound Engineering.
- Each absorbed pattern has a clear home in exactly one surface (skill file, live-doc scaffold, or reference scaffold).
- No duplicate authorities, no compatibility shims, no stale references to the previous batches.
- Root live docs reflect this upgrade as the active objective.
